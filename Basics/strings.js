// Note-anthing inside '',"",`` is called String
// Declairation
/*const str = new String("Kamal");
console.log(str)-->[String: 'Kamal']
accesing character-
str[0]-->K  bz js use zero based indexing
*/

/*
let name="dk";
let age=27;

console.log(`hi my name is ${name} and my age is ${age}`)  ----> srtring interpolation syntax
*/

// see mdn to get string method and properties

//* Note -- string cant change the original value because it is call by value(primitive data type use stack)
/*
str.substring(0,4) // take only positie val
str.slice(-8,4) //take both +ve and -ve value
str.split('seprator') ---> it will return array  ********vvimp
*/